# 杭职大继教院进校系统 - 总部署指南

覆盖三部分：**Cloudflare Workers 管理后台**、**本机/局域网 admin-server**、**微信小程序**。

也可双击根目录 `部署.bat` 按菜单选择部署方式。

---

## 架构一览

| 部分 | 位置 | 部署方式 | 地址 |
|------|------|----------|------|
| Web 后台（线上） | `webadmin/cloudflare` | Cloudflare Workers（免费） | `https://hgd-edu-admin.*.workers.dev` |
| Web 后台（本机） | `webadmin/admin-server` | 本机/局域网 Node 服务 | `http://localhost:3000` |
| 微信小程序 + 云函数 | `wxminiprogram` | 微信开发者工具上传/发布 | 微信内搜索小程序 |

数据主体在**微信云开发**（环境 ID：`cloud1-d6gio7v8iff39bab7`）。Workers 通过 `WX_APP_SECRET` 调用云函数 `adminApi`；admin-server 用 `config.json` 里的 `appSecret` 调同一套云函数。

**敏感信息勿提交 Git：** `config.json`、`.token-secret`、真实 `WX_APP_SECRET`。仓库已提供 `.gitignore` 与 `config.example.json`。

---

## 一、Cloudflare Workers 部署（你来跑命令）

### 前置
1. 注册/登录 [Cloudflare](https://dash.cloudflare.com)
2. 本机已装 Node.js 20+（当前环境 Node v24 已可用）
3. 微信小程序 **AppSecret**：[mp.weixin.qq.com](https://mp.weixin.qq.com) → 开发 → 开发管理 → 开发设置

### 一键（推荐）
在 PowerShell 中执行：

```powershell
powershell -ExecutionPolicy Bypass -File "G:\杭职大继教院进校系统1\webadmin\cloudflare\deploy.ps1"
```

脚本会依次：`npm install` → `wrangler login`（浏览器授权）→ 创建 KV → 填 id 后继续 → `wrangler secret put WX_APP_SECRET` → `wrangler deploy`。

### 手动等价命令
```powershell
cd "G:\杭职大继教院进校系统1\webadmin\cloudflare"
npm install
npx wrangler login
npx wrangler kv namespace create ADMIN_KV
# 把返回的 id 写入 wrangler.toml → [[kv_namespaces]].id
npx wrangler secret put WX_APP_SECRET   # 粘贴 AppSecret
npx wrangler deploy
```

### 部署后检查
- 打开输出的 `*.workers.dev`，用 `admin` / `admin123` 登录（KV 首次登录会创建默认管理员）
- 学员列表、入校申请能加载 = 云函数连通正常
- 默认账号首次登录后请在「账户管理」改密

### 更新部署（改完代码再发）
```powershell
cd "G:\杭职大继教院进校系统1\webadmin\cloudflare"
npx wrangler deploy
```

### 免费额度
每天 100,000 次请求、10ms CPU、免费 SSL、无限带宽——校园管理场景足够。

---

## 二、admin-server（本机/局域网）

Express 服务不能直接跑在 Workers 上；线上用上面的 cloudflare 版，本机用本服务。

```powershell
powershell -ExecutionPolicy Bypass -File "G:\杭职大继教院进校系统1\webadmin\admin-server\start.ps1"
```

或双击 `启动管理后台.bat`。浏览器访问 `http://localhost:3000`。

**配置云密钥：** 复制 `config.example.json` → `config.json`，填入 `appSecret`（仓库里的真实 `config.json` 已在 `.gitignore` 中，不会进 Git）。

**局域网供同事访问：** `server.js` 若绑定 `0.0.0.0`，同事访问 `http://你的内网IP:3000`；注意 Windows 防火墙放行 3000。

---

## 三、GitHub 推送（代码托管）

根目录已 `git init`，并备好 `.gitignore`。在 GitHub 建空仓库后执行其一：

```powershell
# 菜单方式
双击 部署.bat → 选 3

# 或脚本
powershell -ExecutionPolicy Bypass -File "G:\杭职大继教院进校系统1\webadmin\push-github.ps1" -RepoUrl https://github.com/<你的用户名>/<仓库名>.git
```

推送前自查：`git status` 确认 **没有** `config.json`、`.token-secret`、`data/`、`node_modules/`。

---

## 四、微信小程序

### 1. 云开发（若已开通可跳过）
微信开发者工具 → 云开发 → 确认环境 ID 为 `cloud1-d6gio7v8iff39bab7`（或改 `miniprogram/app.js` 与 `project.config.json`）。

### 2. 数据库集合
创建集合：`students`、`users`、`entry_requests`、`admins`（按需）。权限建议：所有用户可读，仅创建者可写（业务写操作走云函数）。

### 3. 部署云函数
在开发者工具中对每个目录右键 → **上传并部署：云端安装依赖**：
- `cloudfunctions/importStudents`
- `cloudfunctions/initAdmin`
- `cloudfunctions/adminApi`
- `cloudfunctions/getExcelTemplate`

右键 `initAdmin` → 在终端中调用 → 看到「默认管理员账号创建成功」。

### 4. 本地预览
项目路径选 `wxminiprogram/`，AppID 使用 `wxcde7f3fd1d5fc715`（或你的），点「预览」扫码。

### 5. 正式发布
1. 工具右上角「上传」→ 版本号如 `1.0.0`
2. [mp.weixin.qq.com](https://mp.weixin.qq.com) → 管理 → 版本管理 → **提交审核**
3. 审核通过（约 1–3 天）→ **发布**

---

## 五、常见问题

| 问题 | 处理 |
|------|------|
| `wrangler deploy` 报 KV id 无效 | 先 `wrangler kv namespace create ADMIN_KV`，把 id 写入 `wrangler.toml` |
| 登录 401 / 云函数报错 | `npx wrangler secret put WX_APP_SECRET` 是否设置成功；AppSecret 是否被重置过 |
| Workers 打开是旧页面 | Cloudflare 缓存：无缓存则强刷；改的是本地 HTML 则需重新 `wrangler deploy` |
| admin-server 端口占用 | 改 `server.js` 末尾 `PORT`，或结束占用进程 |
| 推送后线上没变 | Workers 要再 `wrangler deploy`；小程序要重新「上传」并走版本管理 |
| 密钥泄露进 Git | 立即在微信后台重置 AppSecret，并从历史中清除文件 |

---

## 六、推荐发布顺序

1. 小程序云函数 + 数据库就绪  
2. `webadmin/cloudflare` → Workers 部署 → 网页登录测通  
3. 本机需要时再开 admin-server  
4. `git push` 代码托管  
5. 小程序上传 → 审核 → 发布  

技术支持：有问题联系系统管理员。
